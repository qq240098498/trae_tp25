## 1. 架构设计

```mermaid
graph TD
    A["前端 React + TypeScript"] --> B["状态管理 Zustand"]
    A --> C["路由 React Router"]
    A --> D["UI Tailwind CSS"]
    A --> E["图表 Recharts"]
    A --> F["地图 Leaflet (OpenStreetMap)"]
    A --> G["本地存储 LocalStorage + IndexedDB"]
    G --> H["停车记录数据"]
    G --> I["车位照片数据 (Base64)"]
```

## 2. 技术描述
- **前端**：React@18 + TypeScript + Vite
- **初始化工具**：vite-init
- **样式**：tailwindcss@3
- **路由**：react-router-dom
- **状态管理**：zustand
- **图表库**：recharts
- **地图**：leaflet + react-leaflet（使用 OpenStreetMap 免费瓦片，无需 API Key）
- **图标**：lucide-react
- **数据存储**：LocalStorage 存储结构化数据，照片以 Base64 存入 IndexedDB（或 LocalStorage 限制大小）
- **后端**：无（纯前端应用，数据本地持久化）

## 3. 路由定义
| 路由 | 用途 |
|-------|---------|
| / | 首页/仪表盘，月度概览和最近记录 |
| /records | 停车记录列表 |
| /records/new | 新增停车记录 |
| /records/:id | 停车记录详情/编辑 |
| /statistics | 费用统计分析 |
| /parking-spot | 车位记录/取车页面 |

## 4. 数据模型

### 4.1 数据模型定义

```mermaid
erDiagram
    PARKING_RECORD {
        string id "主键"
        string date "停车日期 YYYY-MM-DD"
        string locationName "地点名称"
        number lat "纬度"
        number lng "经度"
        number duration "停车时长(分钟)"
        number amount "缴费金额(元)"
        string paymentMethod "缴费方式: cash/qrcode/app"
        string notes "备注"
        string createdAt "创建时间"
    }
    
    PARKING_SPOT {
        string id "主键"
        string spotNumber "车位编号 如B2-035"
        string floor "楼层"
        string area "区域"
        string photo "车位照片 Base64"
        string notes "备注"
        string createdAt "创建时间"
        boolean isActive "是否为当前车位"
    }
```

### 4.2 数据结构（TypeScript 类型）

```typescript
// 缴费方式
type PaymentMethod = 'cash' | 'qrcode' | 'app';

// 停车记录
interface ParkingRecord {
  id: string;
  date: string;
  locationName: string;
  lat: number | null;
  lng: number | null;
  duration: number;
  amount: number;
  paymentMethod: PaymentMethod;
  notes?: string;
  createdAt: string;
}

// 车位记录
interface ParkingSpot {
  id: string;
  spotNumber: string;
  floor?: string;
  area?: string;
  photo?: string;
  notes?: string;
  createdAt: string;
  isActive: boolean;
}

// 按地点统计
interface LocationStats {
  locationName: string;
  totalAmount: number;
  count: number;
}

// 月度统计
interface MonthlyStats {
  month: string;
  totalAmount: number;
  count: number;
}
```
